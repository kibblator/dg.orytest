name: Test Workflow

on:
  workflow_dispatch:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: oryd/ory:v1.1.0
      options: --user root
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Say hello
        run: |
          ./scripts/say_hello.sh
